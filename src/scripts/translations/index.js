/**
 * Archivo principal de traducciones
 * Exporta todas las traducciones organizadas por módulos
 */

import { es as esTranslations } from './es.js';
import { en as enTranslations } from './en.js';

export const translations = {
    es: esTranslations,
    en: enTranslations
};

export { esTranslations, enTranslations };
